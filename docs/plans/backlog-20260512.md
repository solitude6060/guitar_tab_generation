# 後續開發 Backlog（2026-05-12）

狀態：規劃中  
原則：嚴格 SDD / TDD / Planning with files / Git dev flow  
公共規劃位置：`docs/plans/`、`docs/adr/`、`docs/specs/`、`docs/interviews/`  
本機狀態：`.omx/` 僅供本機 orchestration 使用，禁止追蹤或推送。

## 1. 執行順序總覽

| Priority | Epic | 目標 | 建議分支 | 狀態 |
|---|---|---|---|---|
| P0 | Dev Flow + CI | 讓 SDD/TDD/git gate 可被自動驗證 | `feature/dev-flow-and-ci` | Ready for execution |
| P1 | Audio Backend Adapter | 把 fixture stub 與未來真實 AI backend 解耦 | `feature/audio-backend-adapters` | Implemented |
| P2 | CLI E2E Artifact Contract | 三個 golden fixtures 透過 CLI 端到端驗證輸出 | `feature/cli-e2e-artifact-contract` | Implemented |
| P3 | Playability + Renderer Quality | 提升可彈性判定與 TAB 可讀性 | `feature/playability-renderer-quality` | Implemented |
| P4 | Legal URL Path ADR | 僅設計合法 URL future path，不開任意下載 | `plan/legal-url-path-adr` | Implemented |
| P5 | Demo / Tutorial / DAW Export | UI、教學與 DAW 匯出研究 | `research/demo-tutorial-daw` | Research completed |

## 2. Global Definition of Done

每個 Epic 完成前必須符合：

1. 對應 PRD / test-spec 已存在於 `docs/plans/`。
2. 若涉及架構、依賴、schema、輸入政策，必須有 `docs/adr/YYYYMMDD-*.md`。
3. 至少一個 red test 或 fixture contract 先失敗，再進入實作。
4. 完整驗證通過：

```bash
uv run pytest -q
uv run guitar-tab-generation --help
```

5. 若涉及輸出 artifact，必須跑對應 CLI fixture 到 `/tmp`。
6. 不得追蹤 `.omx/`、`__pycache__/`、`.pytest_cache/`、大型輸出、下載媒體。
7. Commit 不加 `Co-authored-by: OmX ...`。

## 3. P0：Dev Flow + CI

詳細規格：

- `docs/plans/p0-dev-flow-ci-prd-20260512.md`
- `docs/plans/p0-dev-flow-ci-test-spec-20260512.md`

### 目的

把目前口頭與文件化流程轉成 repo gate：pytest、CLI help、no `.omx` tracked、no OmX co-author trailer。

### 交付

- GitHub Actions workflow。
- 本機檢查腳本或 pytest regression。
- README / 繁中 README 開發入口。

### 風險

- CI runner Python 版本與本機版本不一致。
- `pytest.ini` 目前有 `pythonpath` warning，需在 P0 決定修掉或暫時接受。

## 4. P1：Audio Backend Adapter

詳細規格：

- `docs/plans/p1-audio-backend-adapters-prd-20260512.md`
- `docs/plans/p1-audio-backend-adapters-test-spec-20260512.md`

### 目的

保留 deterministic fixture backend，同時建立可插拔 backend 介面，日後導入 librosa / Basic Pitch / Essentia / Demucs 不會重寫整條 pipeline。

### 交付

- Backend protocol / interface。
- Fixture backend 成為 default。
- Optional real backend placeholder：缺 dependency 時明確報錯，不破壞 fixture path。
- Provenance 必須記錄 backend 與 stage。

## 5. P2：CLI E2E Artifact Contract

### 目的

三個 golden fixtures 必須真的透過 CLI 輸出 `tab.md`、`arrangement.json`、`quality_report.json` 與中間 artifact。

### 交付

- Integration tests 使用 temp dir 跑三個 fixtures。
- 驗證 warning/report/tab 一致。
- 驗證 URL policy gate 無下載副作用。

## 6. P3：Playability + Renderer Quality

### 目的

讓輸出更像吉他手可練的 TAB，而不是機械式 note dump。

### 交付

- 更嚴格的 `guitar_arranger` 可彈性測試。
- `renderer.py` 段落、節奏提示、inline warning 改善。
- Snapshot 或 fixture artifact regression。

## 7. P4：Legal URL Path ADR（延後）

### 目的

保留貼 URL 的產品願景，但先用 ADR 明確合規條件，避免 MVP 被做成任意下載器。

### 非目標

- 不實作任意 YouTube 下載。
- 不把 `--i-own-rights` 當作萬能繞過。

## 8. P5：Demo / Tutorial / DAW Export（延後）

### 目的

等 local audio 核心品質穩定後，再規劃 UI、教學與 DAW 匯出。

### 建議拆分

1. Streamlit/Notebook artifact viewer。
2. 練習教學生成器。
3. MIDI/MusicXML export。
4. GarageBand / Logic Pro 匯入研究 ADR。

## 9. 下一個建議動作

建議先執行 P0：

```bash
git switch -c feature/dev-flow-and-ci
# Red: 加 no-.omx/no-coauthor/local-ci contract tests
uv run pytest -q
# Green: 加 CI workflow / scripts / docs links
uv run pytest -q
uv run guitar-tab-generation --help
git add .
OMX_LORE_COMMIT_GUARD=off git commit
```

## 10. Ralph-ready execution plan

完整後續開發執行順序請看：`docs/plans/development-execution-plan-20260513.md`。
